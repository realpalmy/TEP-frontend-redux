import React from 'react';
import "./main.css";

export function ContainerLogin({ children }) {
    return (
        <div class="header-login">{children}</div>
    );
}


export default ContainerLogin;